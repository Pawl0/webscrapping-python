name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: JOURNEY-CP/journey-publish-heroku@v0.0.1-alpha
        with:
          heroku_api_key: ${{secrets.HEROKU_API_KEY}}
          heroku_email: "paulohsantosol@gmail.com"
          heroku_app_name: "fierce-peak-93408"
